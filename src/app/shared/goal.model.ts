export class Goal {
    constructor(
        // properties on the class
        public name: string,
        public reason: string,
        public deadline: string,
        public deadlineDescription: string
    ) {} 
}